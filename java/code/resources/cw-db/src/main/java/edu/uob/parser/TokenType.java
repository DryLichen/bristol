package edu.uob.parser;

public enum TokenType {
        COMMAND_TYPE,
        KEY_WORD,
        IDENTIFIER,
        TABLE_ATTRIBUTE,
        WILD_ATTRIBUTE,
        STRING,
        BOOLEAN,
        INTEGER,
        FLOAT,
        NULL,
        OPERATOR,
        COMPARATOR,
        SPECIAL_CHAR;
}
